/**
 * Created by long on 15/10/23.
 */

var flax = flax || {};
flax.Module = flax.Module || {};

flax.Module.Action = {
    runAction: function (act) {
        //todo
        flax.log("***** Does not support actions now!")
    },
    stopAction: function (act) {
        //todo
        flax.log("***** Does not support actions now!")
    }
}